OPENSHELL_MANAGER_TOKEN=<openssl rand -hex 24 生成，与 codeaudit/env 中 OPENSHELL_MANAGER_TOKEN 保持同一值>
# 网关 gRPC 端点(缺省经 host-gateway 别名;自定义域名时改为 <域名>:8080 并自行加 extra_hosts)
OPENSHELL_GATEWAY_ENDPOINT=host.docker.internal:8080
