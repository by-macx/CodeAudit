[pytest]
# tests/samples/ = 被测语料（真实第三方源码树, E2E 扫描目标物），不是项目测试代码。
# 不排除会导致: R1 检查把 googletest 文档当内嵌proto、目录卫生把第三方 debug.lua 当残渣、
# G2 把 gmock 自带测试脚本当项目用例收集（2026-08-27 模式D大样本实测暴露）。
# 与 verify.sh 的 .toolchain 排除同理: 语料目录不参与门禁扫描（ADR-126）。
norecursedirs = tests/samples .toolchain .agent/evidence archive
